{
  "name": "blog",
  "version": "1.0.0",
  "description": "Blog website built with Express and React.",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd client && npm install && npm run build",
    "start": "node src/server.js"
  },
  "author": "Michał Janik",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "mongodb": "^7.0.0"
  }
}
